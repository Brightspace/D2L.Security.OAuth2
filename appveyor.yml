
image: Visual Studio 2019

version: '{build}-{branch}'
environment:
  ASSEMBLY_VERSION: 10.0.0

before_build:
  - dotnet restore

configuration: Release
platform: Any CPU

# nuget:
#  account_feed: true

artifacts:
- path: '**\*.nupkg'
  name: NuGet

branches:
  only:
    - master
