namespace D2L.Security.OAuth2.Validation.Jwks {
	internal static class Constants {

		// Keys are good for one hour from when they are fetched
		internal const int KEY_MAXAGE_SECONDS = 3600;
	}
}
